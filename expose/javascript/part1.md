1. it prints 'values added: 20'
2. it prints 'final result: 20' 
3. Because it creates weird scoping issues within JavaScript, such as unexpected bugs or accidental overwrites.
4. it prints 'values added: 20' 
5. it doesn't print anything but throws an error since let was declared inside of the block and therefore the second result can't access it. 
6. it prints a typeerror since it tried reassigning a constant variable. 
7. It never reaches since in line 7 it stops the execution, therefore creating an error, but it's also not accessible. 
